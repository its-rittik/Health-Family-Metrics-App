import 'package:flutter/material.dart';

class FamilyComparisonChart extends StatelessWidget {
  const FamilyComparisonChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Family comparison is disabled.'),
    );
  }
} 